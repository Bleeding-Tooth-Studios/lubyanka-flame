import { PADDING } from "./uiconsts.padding";

export const HEALTH_BAR_WIDTH: number = 8;
export const HEALTH_BAR_HEIGHT: number = 300;
export const HEALTH_BAR_BACKGROUND_SIZE: UDim2 = UDim2.fromOffset(
	HEALTH_BAR_WIDTH + PADDING.XS * 2,
	HEALTH_BAR_HEIGHT + PADDING.XS * 2,
);
