import React, { PropsWithChildren } from "@rbxts/react";

function AutoLayout(props: PropsWithChildren<{}>) {
	return <frame BackgroundTransparency={1} BorderSizePixel={0}></frame>;
}

export default AutoLayout;
